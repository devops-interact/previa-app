# ============================================================
# PREV.IA Backend — Environment Configuration
# ============================================================
# Copy to .env for local development.
# NEVER commit real credentials to version control.
# ============================================================

# --- LLM Provider (Required) --------------------------------
# Anthropic Claude API — agent reasoning, DOF parsing
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_YOUR_KEY
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# --- Database (SQLite for MVP) ------------------------------
# Local: SQLite file in backend/data/
DATABASE_URL=sqlite+aiosqlite:///./data/previa.db

# --- Demo / Test Account ------------------------------------
# Seeded on first startup. MUST disable before production.
DEMO_USER_EMAIL=user@product.test
DEMO_USER_PASSWORD=1234
DEMO_USER_ROLE=analyst

# --- CORS (Required for browser requests) -------------------
# Comma-separated origins that may call the API (no trailing slashes).
# If missing, login from Vercel will fail with "CORS request failed".
# Production (Railway): add your Vercel frontend URL, e.g.:
#   CORS_ALLOWED_ORIGINS=https://previa-app.vercel.app,http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# --- Third-Party Data APIs (Optional) -----------------------
# Reachcore — Real-time Art. 69-B API
REACHCORE_API_KEY=

# --- CAPTCHA Service (Optional) -----------------------------
# 2Captcha — SAT certificate portal automation
CAPTCHA_API_KEY=

# --- Email Notifications (Optional) -------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL_TO=fiscal-team@yourcompany.com

# --- Application Settings -----------------------------------
ALERT_THRESHOLD_CRITICAL=80
ALERT_THRESHOLD_HIGH=60
DATA_STALENESS_WARNING_DAYS=7
SAT_RATE_LIMIT_SECONDS=1.5
LOG_LEVEL=INFO
