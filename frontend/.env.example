# ============================================================
# Prevify Frontend — Environment Configuration
# ============================================================

# Backend API base URL (no trailing slash). Required for login and API calls.
# - Local dev:  http://localhost:8000
# - Vercel + Railway: use your Railway service PUBLIC URL, e.g. https://previa-app-production-xxxx.up.railway.app
#   Do NOT use Railway internal hostnames (*.railway.internal) — they are not reachable from the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth.js configuration (for future OAuth integration)
NEXTAUTH_SECRET=replace-with-random-32-char-string
NEXTAUTH_URL=http://localhost:3000
